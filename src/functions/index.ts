// Main module, only if you want to use the functions

// Imports
import {
    clean,
    normalize,
    percentage,
} from './functions'

import {
    fixHexCode,
    getBlueValue,
    getGreenValue,
    getRedValue,
    hsl,
    hslToRgb,
    isValidHexCode,
    light,
    normalizeColorValue,
    normalizeHSLValue,
    rgb,
    rgbToHsl,
} from './color-functions'

// Exports
export {
    clean,
    normalize,
    percentage,

    fixHexCode,
    getBlueValue,
    getGreenValue,
    getRedValue,
    hsl,
    hslToRgb,
    isValidHexCode,
    light,
    normalizeColorValue,
    normalizeHSLValue,
    rgb,
    rgbToHsl,
}